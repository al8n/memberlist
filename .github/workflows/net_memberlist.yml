name: net_memberlist

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README'
      - 'COPYRIGHT'
      - 'LICENSE*'
      - '**.md'
      - '**.txt'
  pull_request:
    paths-ignore:
      - 'README'
      - 'COPYRIGHT'
      - 'LICENSE*'
      - '**.md'
      - '**.txt'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  rustfmt:
    name: rustfmt
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable && rustup component add rustfmt
    - name: Check formatting
      run: cargo fmt --all -- --check
      working-directory: transports/net


  # Apply clippy lints
  clippy:
    name: clippy
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable && rustup component add clippy
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Apply clippy lints (unix)
      run: cargo clippy --all-features
      working-directory: transports/net

  build:
    name: build
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update nightly --no-self-update && rustup default nightly
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run build (unix)
      run: cargo hack build --feature-powerset --exclude-features tokio,async-std,smol,serde,rkyv,dns-over-h3,dns-over-quic,dns-over-https-rustls,dns-over-rustls,dns-over-openssl,dns-over-native-tls,dns-webpki-roots,dns-native-certs,dnssec-openssl,dnssec-ring,dnssec,metrics,quic,quinn,s2n
      working-directory: memberlist

  tokio-tcp-test:
    name: tokio-tcp-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,tokio,tcp,metrics -- --test-threads 1
      working-directory: memberlist
  
  async-std-tcp-test:
    name: async-std-tcp-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,async-std,tcp,metrics -- --test-threads 1
      working-directory: memberlist
  
  smol-tcp-test:
    name: smol-tcp-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,smol,tcp,metrics -- --test-threads 1
      working-directory: memberlist

  tokio-tls-test:
    name: tokio-tls-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,tokio,tls,metrics -- --test-threads 1
      working-directory: memberlist
  
  async-std-tls-test:
    name: async-std-tls-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,async-std,tls,metrics -- --test-threads 1
      working-directory: memberlist
  
  smol-tls-test:
    name: smol-tls-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,smol,tls,metrics -- --test-threads 1
      working-directory: memberlist

  tokio-native-tls-test:
    name: tokio-native-tls-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,tokio,native-tls,metrics -- --test-threads 1
      working-directory: memberlist
  
  async-std-native-tls-test:
    name: async-std-native-tls-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,async-std,native-tls,metrics -- --test-threads 1
      working-directory: memberlist
      if: matrix.os != 'windows-latest'
  
  smol-native-tls-test:
    name: smol-native-tls-test
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Install Rust
      run: rustup update stable && rustup default stable
    - name: Install OpenSSL (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: |
        echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append
        vcpkg install openssl:x64-windows-static-md
    - name: Setup loopback interface (Windows)
      if: matrix.os == 'windows-latest'
      shell: powershell
      run: ci\setup_subnet_windows.ps1
    - name: Setup loopback interface (MacOS)
      if: matrix.os == 'macos-latest'
      run: ci/setup_subnet_macos.sh
    - name: Setup loopback interface (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: ci/setup_subnet_ubuntu.sh
    - name: Install cargo-hack
      run: cargo install cargo-hack
    - name: Cache ~/.cargo
      uses: actions/cache@v3
      with:
        path: ~/.cargo
        key: ${{ matrix.os }}-coverage-dotcargo
    - name: Run test (Unix)
      run: cargo test --no-default-features --features compression,encryption,test,smol,native-tls,metrics -- --test-threads 1
      working-directory: memberlist


